diff --git a/configure.ac b/configure.ac
index 5a3dbec..2def879 100644
--- a/configure.ac
+++ b/configure.ac
@@ -813,7 +813,8 @@ if test "x$enable_tracker_applet" != "xno" ; then
 		     gtk+-2.0    >= $GTK_REQUIRED
         	     dbus-1      >= $DBUS_REQUIRED
         	     dbus-glib-1 >= $DBUS_REQUIRED
-		     libnotify   >= $LIBNOTIFY_REQUIRED],
+		     libnotify   >= $LIBNOTIFY_REQUIRED
+		     x11],
 		     [have_tracker_applet=yes], 
    		     [have_tracker_applet=no])
 
diff --git a/src/tracker-extract/Makefile.am b/src/tracker-extract/Makefile.am
index 2511c4f..db006a4 100644
--- a/src/tracker-extract/Makefile.am
+++ b/src/tracker-extract/Makefile.am
@@ -245,6 +245,8 @@ tracker_extract_LDADD = 						\
 	$(GMODULE_LIBS)							\
 	$(GTHREAD_LIBS)							\
 	$(GCOV_LIBS)							\
+	$(DBUS_LIBS)							\
+	$(GIO_LIBS)							\
 	$(GLIB2_LIBS)
 
 dbus_sources = 								\
diff --git a/src/tracker-utils/Makefile.am b/src/tracker-utils/Makefile.am
index acf3e6e..48505e1 100644
--- a/src/tracker-utils/Makefile.am
+++ b/src/tracker-utils/Makefile.am
@@ -17,6 +17,7 @@ libs = 									\
 	$(top_builddir)/src/libtracker-db/libtracker-db.la		\
 	$(GIO_LIBS)							\
 	$(GCOV_LIBS)							\
+	$(DBUS_LIBS)							\
 	$(GLIB2_LIBS)
 
 bin_PROGRAMS = 								\
